@using Cedar.WebPortal.WebMVC4.Helpers

@if (Request.IsAuthenticated)
{
    <text>Welcome <b>@Context.User.Identity.Name</b>! [ @Html.ActionLink(Html.ResourceFor("LogOff"), "LogOff", "Account")
    ]</text>
}
else
{
    @:[ @Html.ActionLink(Html.ResourceFor("LogOn"), "LogOn", "Account") ]
}
